#include <stdio.h>
#include <stdlib.h>

/**
 * sub - subtract two integers
 * @a: first integer
 * @b: second integer
 * Returns: subtration of the two integers
*/

int sub(int a, int b)
{
    return (a - b);
}
